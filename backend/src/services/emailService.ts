/**
 * Affiliate / partner email entry point (templates live in `utils/emailService.ts`).
 */
export {
  sendWelcomeToTeamMemberEmail,
  teamMemberRoleLabel,
  type TeamMemberRoleLabel,
} from "../utils/emailService.js";
